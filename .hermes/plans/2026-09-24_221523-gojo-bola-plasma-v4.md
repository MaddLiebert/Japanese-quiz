# Plan — Gojo Bola v4: Plasma Menyala (ikut referensi JJK)

Sumber: user kirim foto referensi JJK (3 panel 蒼/赫/茈) + **"bagus. tapi kurang, coba lu liat foto ini analisis si bola2 nya"**.

Keputusan clarify: **(1)** "Ikut referensi: tepi lembut menyala (tanpa garis hitam) — paling mirip JJK"; **(2)** "Ya, tiap teknik beda bentuk: 蒼 = orbit rings · 赫 = pita vortex · 茈 = cabang petir + halo".

---

## 1. Goal

Perbaiki **hanya bola** (impact star / 集中線 / オノマトペ / petir tepi **tetap** — sudah "bagus"): ganti cakram cel-shade rata berinti hitam → **bola plasma menyala** sesuai referensi JJK: **inti putih-panas** (bukan void hitam), **tepi lembut** (tanpa ring tinta), dan **bentuk beda per teknik** (蒼 orbit rings · 赫 pita vortex · 茈 cabang petir + halo).

---

## 2. Analisis referensi (hasil zoom vision, 3 panel)

| Panel | Struktur sebenarnya |
|---|---|
| **蒼 (biru)** | inti **putih-panas di TENGAH**; dikelilingi pusaran plasma kacau + **2–3 arc halo** nyelimutin. **Tepi lembut/kabur, TANPA garis hitam, TANPA lubang hitam** |
| **赫 (merah)** | **vortex/torus** — **beberapa pita (ribbon) tebal** merah-orange **mutar membelit** di jalur elips. Inti **putih-pink terang** |
| **茈 (ungu)** | inti **putih-pink terang**, permukaan **bertekstur petir**, **cabang petir ungu** keluar, **cincin halo besar** mengelilingi. Tepi lembut |

**Bola sekarang (salah):** ring tinta keras 4px + band cel-shade rata + pinwheel datar + **inti HITAM** + petir hitam. → **2 hal kebalik** (inti harus putih; tepi harus lembut) + bentuk kurang.

---

## 3. Current context / assumptions

Repo `C:\Users\maddo\Documents\japanese-quiz`. Branch `feat/gojo-pack7-dummy`, **ahead 84, JANGAN push**. Gojo **senyap**.

**File yang diubah (hanya 3):**
- `src/features/effects/gojoFx.js` — logika murni
- `src/features/effects/gojoFx.test.js` — test
- `src/features/effects/GojoBurst.jsx` — komponen render

**Yang TIDAK diubah:** impact star, 集中線, screentone, オノマトペ, flash, petir tepi, retak, teks teknik, posisi bola (ao kanan / aka kiri / murasaki tabrakan tengah), `gojoSpheres`, suara.

**Aturan:** animasi hanya `transform`+`opacity` (+`pathLength`); outline pakai stroke/ring (bukan blur); `prefers-reduced-motion`; palet terang `#f3f0e8` / gelap `#141517`.

---

## 4. Spec FINAL (terkunci)

| Aspek | Keputusan |
|---|---|
| Inti bola | **PUTIH-panas** (`GOJO_CORE = #ffffff`) — bukan void hitam |
| Tepi bola | **Lembut menyala** (gradient → transparent), **TANPA ring tinta** |
| 蒼 | **orbit rings** (2–3 ellipse miring, muter pelan) |
| 赫 | **pita vortex** (3–4 ellipse busur tebal membelit, mutar) |
| 茈 | **cabang petir** (tendril bercabang) + **halo besar** |
| Warna | Kanon (biru/merah/ungu) + glow |
| Posisi | Tetap (ao kanan · aka kiri · murasaki tabrakan tengah) |
| Elemen lain | impact star / 集中線 / オノマトペ / petir tepi **tetap** |

**Konstanta baru:** `GOJO_CORE = '#ffffff'`.

---

## 5. Architecture / approach

`gojoFx.js`: tambah `GOJO_CORE` + 5 fungsi murni (`gojoSphereShape`, `gojoOrbitRings`, `gojoRibbons`, `gojoTendrils`, `gojoHalo`). `GojoBurst.jsx`: ganti blok render bola — buang ring tinta + void hitam + pinwheel keras + petir hitam; pasang badan plasma (inti putih → tepi lembut) + orbit rings / ribbons / tendrils / halo sesuai teknik.

---

## 6. Step-by-step tasks

> Dari `C:\Users\maddo\Documents\japanese-quiz` (bash). **Commit tiap fase, JANGAN push.**

### Phase 0 — Baseline
```bash
git status --short
npm test 2>&1 | grep -E "^ℹ (tests|pass|fail)"
```
Harapan: `tests 127 / pass 127 / fail 0`.

---

### Phase 1 — TDD: bentuk bola di `gojoFx.js`

**1.1 (RED)** Tambah ke `gojoFx.test.js` (impor + blok test):
```js
// tambah ke import: GOJO_CORE, gojoSphereShape, gojoOrbitRings, gojoRibbons, gojoTendrils, gojoHalo

// ── Bola plasma: bentuk per teknik (mengikuti referensi JJK) ────────────────
test('GOJO_CORE = putih (inti menyala, bukan void hitam)', () => {
  assert.equal(GOJO_CORE.toLowerCase(), '#ffffff');
});

test('gojoSphereShape: ao=rings, aka=ribbons, murasaki=tendrils+halo', () => {
  const ao = gojoSphereShape('ao');
  assert.ok(ao.rings >= 2, 'ao = orbit rings');
  assert.equal(ao.ribbons, 0);
  assert.equal(ao.tendrils, 0);
  const aka = gojoSphereShape('aka');
  assert.ok(aka.ribbons >= 3, 'aka = pita vortex');
  assert.equal(aka.rings, 0);
  const mura = gojoSphereShape('murasaki');
  assert.ok(mura.tendrils >= 4, 'murasaki = cabang petir');
  assert.ok(mura.halo >= 1, 'murasaki = halo besar');
});

test('gojoOrbitRings: deterministik, jumlah = shape, elips (rx>ry)', () => {
  const a = gojoOrbitRings('ao', 1, () => 0.5);
  const b = gojoOrbitRings('ao', 1, () => 0.5);
  assert.deepEqual(a, b);
  assert.equal(a.length, gojoSphereShape('ao').rings);
  for (const r of a) {
    assert.ok(r.rx > r.ry, 'harus elips (miring)');
    assert.ok(r.width > 0 && r.dur > 0);
  }
  assert.equal(gojoOrbitRings('aka', 1, () => 0.5).length, 0, 'aka tanpa rings');
});

test('gojoRibbons: deterministik, jumlah = shape, pita tebal', () => {
  const a = gojoRibbons('aka', 1, () => 0.5);
  const b = gojoRibbons('aka', 1, () => 0.5);
  assert.deepEqual(a, b);
  assert.equal(a.length, gojoSphereShape('aka').ribbons);
  for (const r of a) assert.ok(r.width >= 2, 'pita harus tebal');
});

test('gojoTendrils: deterministik, titik valid 0..100', () => {
  const a = gojoTendrils('murasaki', 1, () => 0.5);
  const b = gojoTendrils('murasaki', 1, () => 0.5);
  assert.deepEqual(a, b);
  assert.equal(a.length, gojoSphereShape('murasaki').tendrils);
  for (const t of a) {
    assert.ok(t.points.length >= 4, 'tendril minimal 4 titik');
    for (const [x, y] of t.points) {
      assert.ok(x >= 0 && x <= 100 && y >= 0 && y <= 100, `titik (${x},${y}) di luar 0..100`);
    }
  }
});

test('gojoHalo: hanya murasaki, 1 cincin', () => {
  assert.equal(gojoHalo('murasaki', 1, () => 0.5).length, 1);
  assert.equal(gojoHalo('ao', 1, () => 0.5).length, 0);
});
```

**1.2 (RED — verifikasi gagal):**
```bash
node --test src/features/effects/gojoFx.test.js 2>&1 | grep -E "^ℹ (tests|pass|fail)|does not provide"
```
Harapan: **GAGAL** — `does not provide an export named 'GOJO_CORE'`.

**1.3 (GREEN)** Tambah ke `gojoFx.js`:
```js
// ── Inti bola: putih-panas (bukan void hitam) ───────────────────────────────
export const GOJO_CORE = '#ffffff';

// ── Bentuk bola per teknik (mengikuti referensi JJK) ────────────────────────
//   ao       → orbit rings (2–3 cincin elips nyelimutin)
//   aka      → pita vortex (ribbon tebal membelit)
//   murasaki → cabang petir (tendril) + halo besar
export const gojoSphereShape = (technique) => {
  if (technique === 'ao') return { rings: 3, ribbons: 0, tendrils: 0, halo: 0 };
  if (technique === 'aka') return { rings: 0, ribbons: 4, tendrils: 0, halo: 0 };
  if (technique === 'murasaki') return { rings: 0, ribbons: 0, tendrils: 6, halo: 1 };
  return { rings: 0, ribbons: 0, tendrils: 0, halo: 0 };
};

// Orbit rings (ellipse miring) — ruang 0..100, pusat 50,50.
export const gojoOrbitRings = (technique, seed = 1, rng = Math.random) => {
  const n = gojoSphereShape(technique).rings;
  const out = [];
  for (let i = 0; i < n; i++) {
    out.push({
      id: `${seed}-ring-${i}`,
      rx: 52 + rng() * 16,
      ry: 14 + rng() * 12,        // rx > ry → terlihat miring
      rot: Math.round(rng() * 180),
      width: 1.6 + rng() * 1.4,
      dur: 2.2 + rng() * 1.6,
      delay: rng() * 0.2,
    });
  }
  return out;
};

// Pita vortex (aka): busur elips tebal yang melilit bola.
export const gojoRibbons = (technique, seed = 1, rng = Math.random) => {
  const n = gojoSphereShape(technique).ribbons;
  const out = [];
  for (let i = 0; i < n; i++) {
    out.push({
      id: `${seed}-ribbon-${i}`,
      rx: 46 + rng() * 14,
      ry: 20 + rng() * 18,
      rot: Math.round(rng() * 180),
      width: 3 + rng() * 3,
      dur: 1.8 + rng() * 1.4,
      delay: rng() * 0.15,
      dir: rng() < 0.5 ? 1 : -1,
    });
  }
  return out;
};

// Cabang petir (murasaki): polyline bercabang keluar dari tepi bola.
export const gojoTendrils = (technique, seed = 1, rng = Math.random) => {
  const n = gojoSphereShape(technique).tendrils;
  const out = [];
  for (let i = 0; i < n; i++) {
    const a = rng() * Math.PI * 2;
    const len = 20 + rng() * 25;
    const steps = 4;
    const pts = [];
    for (let s = 0; s <= steps; s++) {
      const t = s / steps;
      const r = 38 + t * len;
      const jit = (rng() * 2 - 1) * 7;
      const aa = a + (rng() * 2 - 1) * 0.5 * t;
      pts.push([clamp100(50 + Math.cos(aa) * (r + jit)), clamp100(50 + Math.sin(aa) * (r + jit))]);
    }
    out.push({ id: `${seed}-tendril-${i}`, points: pts, width: 1.2 + rng() * 1.4, delay: rng() * 0.15 });
  }
  return out;
};

// Halo besar (murasaki): 1 cincin lebar mengelilingi bola.
export const gojoHalo = (technique, seed = 1, rng = Math.random) => {
  const n = gojoSphereShape(technique).halo;
  const out = [];
  for (let i = 0; i < n; i++) {
    out.push({
      id: `${seed}-halo-${i}`,
      rx: 74 + rng() * 10,
      ry: 26 + rng() * 10,
      rot: Math.round(rng() * 40 - 20),
      width: 3 + rng() * 2,
      dur: 3.2 + rng() * 1.4,
    });
  }
  return out;
};
```

**1.4 (GREEN — verifikasi lulus):**
```bash
node --test src/features/effects/gojoFx.test.js 2>&1 | grep -E "^ℹ (tests|pass|fail)"
```
Harapan: semua pass, `fail 0`.

**1.5** Commit:
```bash
git add src/features/effects/gojoFx.js src/features/effects/gojoFx.test.js
git commit -m "feat(gojo): bentuk bola plasma per teknik — orbit rings/ribbons/tendrils+halo, inti putih (GOJO_CORE)"
```

---

### Phase 2 — Render: ganti blok bola di `GojoBurst.jsx`

**2.1** Ubah impor + state:
```js
import {
  gojoTechniqueFor, GOJO_STYLE, gojoParticles, gojoCrackCount, gojoSpheres,
  gojoBolts, gojoStars, GOJO_VOID, GOJO_RIM,
  GOJO_INK, GOJO_FLASH, gojoImpactFocus, gojoImpactStar,
  gojoSpeedLines, gojoHalftone, gojoOno,
  GOJO_CORE, gojoOrbitRings, gojoRibbons, gojoTendrils, gojoHalo,
} from './gojoFx';
```
(perhatikan `gojoSphereBolts` **dibuang** dari impor)

Tambah state:
```js
const [rings] = useState(() => (technique ? gojoOrbitRings(technique, seed) : []));
const [ribbons] = useState(() => (technique ? gojoRibbons(technique, seed) : []));
const [tendrils] = useState(() => (technique ? gojoTendrils(technique, seed) : []));
const [halos] = useState(() => (technique ? gojoHalo(technique, seed) : []));
```

**2.2** Ganti isi `{spheres.map((b, si) => (...))}` dengan versi plasma:
```jsx
{spheres.map((b, si) => (
  <motion.div
    key={`sphere-${b.id}`}
    className="absolute left-1/2 top-1/2"
    style={{ width: b.size, height: b.size, marginLeft: -b.size / 2, marginTop: -b.size / 2, willChange: 'transform, opacity' }}
    initial={reduced ? { x: `${b.anchorVw}vw`, opacity: 1 } : { x: `${b.fromVw}vw`, opacity: 0, scale: 0.6 }}
    animate={reduced
      ? { x: `${b.anchorVw}vw`, opacity: 0 }
      : { x: `${b.anchorVw}vw`, opacity: [0, 1, 1, 0], scale: [0.6, 1, 1.06, 1.15] }}
    transition={reduced ? { duration: 0 } : {
      x: { duration: b.dur, delay: b.delay, ease: [0.16, 1, 0.3, 1] },
      opacity: { duration: b.dur + 0.5, delay: b.delay, times: [0, 0.1, 0.7, 1], ease: 'easeOut' },
      scale: { duration: b.dur + 0.5, delay: b.delay, times: [0, 0.12, 0.6, 1], ease: 'easeOut' },
    }}
  >
    {/* halo lembut (glow menyala, bukan garis) */}
    <div className="absolute rounded-full" style={{
      inset: -b.size * 0.4,
      background: `radial-gradient(circle, ${b.color}66, transparent 68%)`,
    }} />
    {/* badan plasma: inti PUTIH-panas → warna → tepi lembut (tanpa ring tinta) */}
    <div className="absolute inset-0 rounded-full" style={{
      background: `radial-gradient(circle, ${GOJO_CORE} 0 7%, ${b.color} 7% 30%, ${b.color}aa 30% 50%, ${b.color}33 50% 66%, transparent 72%)`,
    }} />
    {/* pusaran plasma (conic lembut, screen blend) */}
    <motion.div className="absolute inset-0 rounded-full" style={{
      background: `conic-gradient(from 0deg, transparent, ${b.color}99 18%, transparent 38%, ${b.color}99 58%, transparent 78%, ${b.color}99)`,
      mixBlendMode: 'screen',
      willChange: 'transform',
    }}
      animate={reduced ? {} : { rotate: 360 }}
      transition={{ duration: 3.4, repeat: Infinity, ease: 'linear' }}
    />
    {/* inti putih terang */}
    <div className="absolute rounded-full" style={{
      inset: b.size * 0.3,
      background: `radial-gradient(circle, ${GOJO_CORE} 0 40%, ${b.color} 62%, transparent 82%)`,
    }} />
    {/* orbit rings / ribbons / tendrils / halo (sesuai teknik) */}
    <svg className="absolute inset-0 w-full h-full overflow-visible" viewBox="0 0 100 100" aria-hidden="true">
      {halos.map((h) => (
        <motion.ellipse key={h.id} cx="50" cy="50" rx={h.rx} ry={h.ry}
          fill="none" stroke={b.color} strokeWidth={h.width} opacity={0.6}
          style={{ transformOrigin: '50% 50%', rotate: `${h.rot}deg` }}
          animate={reduced ? {} : { rotate: [h.rot, h.rot + 360] }}
          transition={{ duration: h.dur, repeat: Infinity, ease: 'linear' }} />
      ))}
      {rings.map((r) => (
        <motion.ellipse key={r.id} cx="50" cy="50" rx={r.rx} ry={r.ry}
          fill="none" stroke={b.color} strokeWidth={r.width} opacity={0.85}
          style={{ transformOrigin: '50% 50%', rotate: `${r.rot}deg` }}
          animate={reduced ? {} : { rotate: [r.rot, r.rot + 360] }}
          transition={{ duration: r.dur, repeat: Infinity, ease: 'linear', delay: r.delay }} />
      ))}
      {ribbons.map((r) => (
        <motion.ellipse key={r.id} cx="50" cy="50" rx={r.rx} ry={r.ry}
          fill="none" stroke={b.color} strokeWidth={r.width} strokeLinecap="round"
          strokeDasharray="44 60" opacity={0.9}
          style={{ transformOrigin: '50% 50%', rotate: `${r.rot}deg` }}
          animate={reduced ? {} : { rotate: [r.rot, r.rot + 360 * r.dir] }}
          transition={{ duration: r.dur, repeat: Infinity, ease: 'linear', delay: r.delay }} />
      ))}
      {tendrils.map((t) => (
        <motion.polyline key={t.id} points={t.points.map((p) => p.join(',')).join(' ')}
          fill="none" stroke={b.color} strokeWidth={t.width}
          strokeLinecap="round" strokeLinejoin="round"
          initial={{ opacity: 0 }}
          animate={reduced ? { opacity: 0.8 } : { opacity: [0, 1, 0.3, 0.9, 0.4] }}
          transition={{ duration: 0.6, repeat: reduced ? 0 : Infinity, repeatDelay: 0.3, delay: t.delay }} />
      ))}
    </svg>
  </motion.div>
))}
```

**2.3** Ledakan inti 茈 (blok `technique === 'murasaki'`) — lembutkan: buang `boxShadow: 0 0 0 4px GOJO_INK`, ganti badan pakai `GOJO_CORE` (putih) → warna → tepi transparan; ring jadi glow (bukan garis tinta):
```jsx
<motion.div key={`mura-core-${fx.id}`} className="absolute left-1/2 top-1/2 rounded-full"
  style={{
    width: 132, height: 132, marginLeft: -66, marginTop: -66,
    background: `radial-gradient(circle, ${GOJO_CORE} 0 18%, ${GOJO_STYLE.murasaki.color} 40%, ${GOJO_VOID} 70%, transparent 82%)`,
    boxShadow: `0 0 60px ${GOJO_STYLE.murasaki.color}`,
    willChange: 'transform, opacity',
  }}
  initial={{ scale: 0, opacity: 0 }}
  animate={reduced ? { scale: 1, opacity: 1 } : { scale: [0, 0, 1.5, 2.1], opacity: [0, 0, 1, 0] }}
  transition={{ duration: reduced ? 0 : 1.0, times: [0, 0.5, 0.66, 1], ease: 'easeOut' }} />
<motion.div key={`mura-ring-${fx.id}`} className="absolute left-1/2 top-1/2 rounded-full"
  style={{
    width: 132, height: 132, marginLeft: -66, marginTop: -66,
    border: `5px solid ${GOJO_STYLE.murasaki.color}`,
    boxShadow: `0 0 24px ${GOJO_STYLE.murasaki.color}`,
    willChange: 'transform, opacity',
  }}
  initial={{ scale: 0.4, opacity: 0 }}
  animate={reduced ? { scale: 0.4, opacity: 0 } : { scale: [0.4, 0.4, 4], opacity: [0, 0, 0.9, 0] }}
  transition={{ duration: reduced ? 0 : 1.15, times: [0, 0.5, 0.62, 1], ease: 'easeOut' }} />
```

**2.4** Verifikasi:
```bash
npm test 2>&1 | grep -E "^ℹ (tests|pass|fail)"
npm run lint >/dev/null 2>&1; echo "lint exit=$?"
npm run build 2>&1 | grep -E "built in|error" | tail -2
```

**2.5** Commit:
```bash
git add src/features/effects/GojoBurst.jsx
git commit -m "feat(gojo): bola plasma menyala — inti putih + tepi lembut, orbit rings/ribbons/tendrils+halo per teknik"
```

---

### Phase 3 — Verifikasi browser (port 5174, JANGAN sentuh 5173)

**3.1** `npm run dev -- --port 5174 --strictPort` → `curl` harapan `200`.
**3.2** Equip Gojo + mulai quiz + jawab #1/#2/#3. Tiap kali cek DOM:
- **Tidak ada ring tinta di bola**: `document.querySelectorAll('div[style*="0 0 0 4px"]').length === 0`
- **Inti putih**: ada elemen bola dengan `background` mengandung `#ffffff` di stop pertama.
- **蒼**: ada `ellipse` orbit (`document.querySelectorAll('svg ellipse').length >= 2`) dan **tidak ada** polygon ribbon.
- **赫**: ada `ellipse` dengan `stroke-dasharray` (ribbon).
- **茈**: ada `polyline` tendril (`>= 4`) + halo `ellipse`.
- **Posisi tetap**: ao x jauh kanan; aka x jauh kiri; murasaki 2 bola konvergen ke tengah.
- **Petir tepi + impact star + オノマトペ tetap ada** (regression check).
**3.3** 0 console error + `document.body.scrollWidth <= innerWidth`.
**3.4** Tema gelap: bola menyala tetap kebaca.
**3.5** `prefers-reduced-motion`: bola tetap render tanpa crash.
**3.6** Screenshot + vision: konfirmasi bola = plasma menyala (inti putih, tepi lembut), bukan cakram berinti hitam.
**3.7** Matikan server 5174.

---

## 7. Tests / validation

- **TDD**: Phase 1 test dulu (RED) → implementasi (GREEN).
- Gerbang: `npm test` (≥127 pass / 0 fail), `npm run lint` (0 error), `npm run build` (✓).
- Test mengunci: `GOJO_CORE` putih, `gojoSphereShape` beda per teknik, rings elips (rx>ry), ribbons tebal, tendrils titik valid, halo hanya murasaki.
- Verifikasi browser: tanpa ring tinta di bola, inti putih, rings/ribbons/tendrils/halo per teknik, posisi tetap, elemen lain tetap, 0 error, 2 tema, reduced-motion.
- **Commit per fase, JANGAN push.**

---

## 8. Risks, tradeoffs, open questions

- **Tepi lembut di tema terang** (`#f3f0e8`): glow biru/merah/ungu tetap kelihatan; kalau kurang tegas, tambah halo alpha.
- **Bola tanpa garis tinta** = sengaja (keputusan user: "paling mirip JJK"). Elemen manga (impact star/集中線/オノマトペ) tetap beri "rasa anime".
- **`mixBlendMode: screen`** pada conic pusaran — di atas bg terang bisa pudar; kalau perlu ganti ke `normal` + alpha.
- **`transformOrigin: 50% 50%`** pada `<ellipse>` — bbox ellipse persis di pusat (50,50) → rotasi benar. Kalau tidak, set `transformBox: 'fill-box'`.
- **`rotate` di `<motion.ellipse>`**: framer-motion menerapkan CSS transform; pastikan `transformOrigin` ikut (sudah di-set di style).
- **Open question**: apakah murasaki (2 bola) perlu tendril di **kedua** bola atau hanya setelah tabrakan? Default: kedua bola (bentuk sama).
